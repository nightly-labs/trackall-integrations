export enum PlatformTag {
  /** Decentralized exchange. */
  Dex = 'dex',
  /** Lending and credit protocols. */
  Lending = 'lending',
  /** Stablecoins and stable-value assets. */
  Stablecoin = 'stablecoin',
  /** Centralized finance products (CeFi). */
  CeFi = 'cefi',
  /** Decentralized finance products and ecosystems. */
  DeFi = 'defi',
  /** Non-fungible token related protocols. */
  NFT = 'nft',
  /** Centralized exchange services. */
  CEX = 'cex',
  /** Wallet applications and providers. */
  Wallet = 'wallet',
  /** Cross-chain bridge / transfer infrastructure. */
  Bridge = 'bridge',
  /** Staking / delegation platforms. */
  Staking = 'staking',
  /** DAO/governance tools and frameworks. */
  Governance = 'governance',
}
